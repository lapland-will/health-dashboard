# 金明训练日志系统 - 使用指南

**创建时间：** 2026-02-04
**系统版本：** v1.0

---

## 🎯 系统功能

### 1. 整合历史数据
- ✅ 自动搜索800-2000天历史数据
- ✅ 支持Excel文件导入（.xlsx）
- ✅ 支持Markdown文件导入（.md）
- ✅ 自动提取训练内容、日期、数据

### 2. 梳理每天训练内容
- ✅ 按日期索引
- ✅ 按训练类型分类
- ✅ 按训练地点分类
- ✅ 全文搜索功能

### 3. 方便查找和查看
- ✅ 按日期范围查询
- ✅ 按关键词搜索
- ✅ 浏览器查看器（HTML）
- ✅ Markdown报告生成

### 4. 方便日常登记
- ✅ 交互式命令行界面
- ✅ 快速添加训练日志
- ✅ 自动索引和分类
- ✅ 自动生成每日报告

---

## 📂 文件结构

```
Personal/Health/
├── training_log_manager.py       # 训练日志管理器（主程序）
├── TrainingLogs/                  # 训练日志目录
│   ├── training_logs.json        # 所有训练日志
│   ├── logs_index.json           # 日志索引
│   ├── training_stats.json       # 统计数据
│   └── training_report_YYYY-MM-DD.md  # 每日报告
└── TrainingData/                 # 历史训练数据
    ├── 金明综合训练表.xlsx       # 主要训练记录
    └── 肺活量测量表.xlsx        # 肺活量数据
```

---

## 🚀 使用方法

### 方法1：交互式命令行（推荐）

```bash
cd Personal/Health
python3 training_log_manager.py
```

**菜单选项：**
1. 添加训练日志
2. 查看训练日志
3. 搜索训练日志
4. 生成统计报告
5. 导入历史数据
6. 打开日志查看器（浏览器）
0. 退出

---

### 方法2：编程方式添加日志

```python
from training_log_manager import TrainingLogManager

# 创建管理器
manager = TrainingLogManager()

# 添加日志
entry = {
    "training_type": "泳池训练（四项基础）",
    "location": "静安体育中心",
    "duration": 90,
    "content": """
    【热身】
    - 陆上热身5分钟
    - 自由泳200米

    【无蹼训练】
    - 25米x4组
    - 50米x3组

    【1000米蛙泳】
    - 总时间：18分05秒
    - 配速：1:48/100m
    """,
    "data": {
        "总时间": "18分05秒",
        "配速": "1:48/100m",
        "平均心率": "148bpm"
    },
    "summary": "训练顺利，配速稳定"
}

# 添加到日志
manager.add_entry("2026-02-04", entry)
```

---

### 方法3：查看日志

```python
# 查看指定日期
log = manager.get_log_by_date("2026-02-04")
print(log)

# 查看最近7天
recent = manager.get_recent_logs(days=7)

# 按关键词搜索
results = manager.search_logs(content_contains="蛙泳")

# 生成报告
report = manager.generate_daily_report("2026-02-04")
print(report)
```

---

### 方法4：导入历史数据

```python
# 导入所有历史数据
manager.import_all_historical_data()

# 或导入指定文件
manager.import_from_excel(Path("TrainingData/金明综合训练表.xlsx"))
manager.import_from_markdown(Path("Notes/training_log.md"))
```

---

## 📝 训练日志格式

### 最小格式
```json
{
  "date": "2026-02-04",
  "training_type": "泳池训练",
  "location": "静安体育中心",
  "duration": 90,
  "content": "今天进行了无蹼和单蹼训练",
  "data": {},
  "summary": "训练顺利"
}
```

### 完整格式
```json
{
  "date": "2026-02-04",
  "training_type": "泳池训练（四项基础）",
  "location": "静安体育中心",
  "duration": 120,
  "content": "【热身】\n陆上热身5分钟\n自由泳200米\n\n【无蹼训练】\n25米x4组：35, 36, 34, 33秒\n50米x3组：1:12, 1:10, 1:11\n\n【单蹼训练】\n50米x4组：42, 41, 43, 41秒\n\n【1000米蛙泳】\n总时间：18分05秒\n配速：1:48/100m\n平均心率：148bpm",
  "data": {
    "无蹼25平均": "34.5秒",
    "单蹼50平均": "41.75秒",
    "1000米总时间": "18分05秒",
    "配速": "1:48/100m",
    "平均心率": "148bpm",
    "最高心率": "162bpm",
    "休息时间": "3分钟"
  },
  "summary": "今天状态很好，无蹼配速稳定，单蹼效率提升。1000米比上次快了10秒，心率控制也不错。继续加油！",
  "personal_bests": {
    "单蹼50米": "41秒",
    "无蹼25米": "33秒"
  }
}
```

---

## 🔍 搜索功能

### 按日期搜索
```python
# 查看特定日期
log = manager.get_log_by_date("2026-02-04")

# 查看日期范围
logs = manager.get_logs_by_date_range("2026-01-01", "2026-02-04")
```

### 按类型搜索
```python
# 只看泳池训练
pool_logs = manager.search_logs(training_type="泳池训练")

# 只看陆地训练
land_logs = manager.search_logs(training_type="陆地训练")
```

### 按关键词搜索
```python
# 搜索包含"蛙泳"的所有记录
breaststroke_logs = manager.search_logs(content_contains="蛙泳")

# 搜索包含"PB"的记录
pb_logs = manager.search_logs(content_contains="PB")
```

---

## 📊 统计功能

### 自动统计
系统自动维护以下统计数据：
- 总训练天数
- 总训练次数
- 最早训练日期
- 最近训练日期
- 按类型统计
- 按地点统计

### 查看统计
```python
stats = manager.generate_statistics()
print(stats)
```

输出示例：
```
# 训练统计报告

## 📊 总体统计

**总训练天数：** 365天
**总训练次数：** 420次
**数据范围：** 2025-02-04 至 2026-02-04

## 🏋️‍♂️ 训练类型分布

- **泳池训练（四项基础）：** 180次
- **1000米蛙泳：** 120次
- **陆地训练：** 80次
- **恢复训练：** 40次

## 📍 训练地点分布

- **静安体育中心：** 200次
- **浦东游泳馆：** 120次
- **户外开放水域：** 20次
```

---

## 🖥️ 浏览器查看器

### 打开查看器
```bash
python3 training_log_manager.py
# 选择 6
```

### 查看器功能
- 显示所有训练统计
- 显示最近30天训练记录
- 美观的卡片式布局
- 响应式设计
- 自动刷新

---

## 💡 使用建议

### 1. 每日训练后立即记录
训练结束后趁记忆新鲜，立即记录：
- 训练内容
- 训练数据（时间、配速、心率）
- 感受和总结

### 2. 详细记录数据
记录详细数据有助于：
- 追踪进步
- 发现问题
- 优化训练
- 回顾历史

### 3. 定期导入历史数据
每周或每月：
- 检查新文件
- 导入数据
- 更新统计
- 备份数据

### 4. 利用搜索功能
- 查找最佳训练日
- 对比不同时期
- 发现训练模式
- 总结经验教训

---

## 🔧 高级功能

### 批量导入
```python
# 导入所有历史数据
manager.import_all_historical_data()

# 自动搜索并导入：
# - 所有Excel文件（.xlsx）
# - 所有Markdown文件（.md）
# - 提取日期和内容
# - 自动分类和索引
```

### 生成报告
```python
# 生成每日报告
manager.generate_daily_report("2026-02-04")

# 生成Markdown统计报告
manager.generate_markdown_report()
```

### 数据备份
```python
# 备份所有数据
import shutil
shutil.copytree("TrainingLogs", f"TrainingLogs_backup_{datetime.now().strftime('%Y%m%d')}")
```

---

## 📱 集成到健康看板

训练日志系统可以与健康看板系统集成：

```python
# 在健康看板中添加训练日志
from training_log_manager import TrainingLogManager

class UltimateHealthDashboard:
    def __init__(self):
        # ... 现有代码 ...
        self.training_log_manager = TrainingLogManager()

    def generate_ultimate_dashboard(self):
        # ... 现有代码 ...

        # 添加训练日志部分
        recent_logs = self.training_log_manager.get_recent_logs(7)
        md += "\n## 📝 最近训练记录\n\n"
        for log in recent_logs:
            md += f"### {log['date']} - {log.get('training_type', '未分类')}\n\n"
            md += f"{log.get('summary', '无总结')}\n\n"
```

---

## ⚠️ 注意事项

1. **数据格式：** 确保日期格式为 YYYY-MM-DD
2. **备份：** 定期备份 TrainingLogs 目录
3. **导入：** 导入前备份现有数据
4. **搜索：** 关键词搜索区分大小写
5. **浏览器：** 查看器需要现代浏览器（Chrome、Firefox、Safari）

---

## 🎉 开始使用

```bash
cd Personal/Health
python3 training_log_manager.py
```

选择功能开始使用！

---

**系统版本：** v1.0
**最后更新：** 2026-02-04
**开发者：** Claude Code for 金明
